{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py shell -c \"from core.models import User; User.objects.filter(username='admin').exists() or User.objects.create_superuser('admin', 'admin@etech.com', 'etech@admin123', role='admin')\" && gunicorn etech.wsgi --log-file -",
    "restartPolicyType": "ON_FAILURE"
  }
}